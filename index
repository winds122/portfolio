<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Roman Pruglo — Customer Success Manager</title>
  <meta name="description" content="Customer Success Manager with 7+ years in Fintech and Crypto. B2B retention, churn reduction, revenue growth." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@2.44.0/tabler-icons.min.css" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #0E1117;
      --bg-card: rgba(255,255,255,0.03);
      --bg-metrics: rgba(45,125,210,0.03);
      --border: rgba(255,255,255,0.08);
      --border-soft: rgba(255,255,255,0.06);
      --accent: #2D7DD2;
      --accent-hover: #2468B5;
      --accent-dim: rgba(45,125,210,0.3);
      --accent-bg: rgba(45,125,210,0.08);
      --accent-border: rgba(45,125,210,0.25);
      --text-primary: #F2F4F8;
      --text-body: #E8EAF0;
      --text-muted: #8A8FA8;
      --text-dim: #6B7394;
      --text-darker: #4A5170;
      --text-faint: #3A4060;
      --text-highlight: #C5D8F5;
      --radius: 8px;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--text-body);
      font-family: 'Inter', sans-serif;
      font-size: 14px;
      line-height: 1.6;
    }

    .mono { font-family: 'JetBrains Mono', monospace; }

    /* ── Nav ── */
    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 48px;
      border-bottom: 0.5px solid var(--border);
      position: sticky;
      top: 0;
      background: rgba(14,17,23,0.95);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      z-index: 100;
    }
    .nav-logo {
      font-family: 'JetBrains Mono', monospace;
      font-size: 13px;
      color: var(--accent);
      letter-spacing: 0.03em;
      text-decoration: none;
    }
    .nav-links { display: flex; gap: 28px; }
    .nav-links a {
      font-size: 11px;
      color: var(--text-muted);
      text-decoration: none;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--text-primary); }

    /* ── Sections ── */
    section { padding: 64px 48px; border-bottom: 0.5px solid var(--border-soft); }

    .section-label {
      font-family: 'JetBrains Mono', monospace;
      font-size: 10px;
      color: var(--accent);
      letter-spacing: 0.16em;
      text-transform: uppercase;
      margin-bottom: 32px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .section-label::after {
      content: '';
      flex: 1;
      height: 0.5px;
      background: var(--accent-dim);
    }

    /* ── Hero ── */
    #hero { padding-top: 80px; padding-bottom: 72px; }
    .hero-eyebrow {
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
      color: var(--accent);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      margin-bottom: 20px;
    }
    .hero-name {
      font-size: 52px;
      font-weight: 600;
      line-height: 1.05;
      letter-spacing: -0.025em;
      color: var(--text-primary);
      margin-bottom: 14px;
    }
    .hero-title {
      font-size: 17px;
      color: var(--text-muted);
      font-weight: 300;
      margin-bottom: 28px;
    }
    .hero-title span { color: var(--accent); font-weight: 400; }
    .hero-summary {
      max-width: 580px;
      font-size: 14px;
      color: var(--text-muted);
      line-height: 1.8;
      margin-bottom: 36px;
    }
    .contacts { display: flex; flex-wrap: wrap; gap: 10px; }
    .chip {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      font-size: 12px;
      color: var(--text-muted);
      background: var(--bg-card);
      border: 0.5px solid var(--border);
      border-radius: 6px;
      padding: 7px 14px;
      text-decoration: none;
      transition: border-color 0.2s, color 0.2s;
    }
    .chip:hover { border-color: var(--accent); color: var(--accent); }
    .chip i { font-size: 15px; }

    /* ── Metrics ── */
    #metrics {
      background: var(--bg-metrics);
      padding-top: 48px;
      padding-bottom: 48px;
    }
    .metrics-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 16px;
    }
    .metric-card {
      background: var(--bg-card);
      border: 0.5px solid var(--border);
      border-radius: var(--radius);
      padding: 20px 18px;
      transition: border-color 0.2s;
    }
    .metric-card:hover { border-color: rgba(45,125,210,0.5); }
    .metric-value {
      font-family: 'JetBrains Mono', monospace;
      font-size: 24px;
      font-weight: 600;
      color: var(--accent);
      line-height: 1.1;
      margin-bottom: 8px;
    }
    .metric-label { font-size: 11px; color: var(--text-dim); line-height: 1.45; }

    /* ── Experience ── */
    .job { margin-bottom: 44px; }
    .job:last-child { margin-bottom: 0; }
    .job-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      flex-wrap: wrap;
      gap: 6px;
      margin-bottom: 4px;
    }
    .job-title { font-size: 15px; font-weight: 500; color: var(--text-primary); }
    .job-period {
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
      color: var(--text-darker);
    }
    .job-company { font-size: 13px; color: var(--accent); margin-bottom: 16px; }
    .job-bullets { list-style: none; display: flex; flex-direction: column; gap: 8px; }
    .job-bullets li {
      font-size: 13px;
      color: var(--text-muted);
      padding-left: 18px;
      position: relative;
      line-height: 1.65;
    }
    .job-bullets li::before {
      content: '—';
      position: absolute;
      left: 0;
      color: var(--text-darker);
      font-family: 'JetBrains Mono', monospace;
      font-size: 12px;
    }
    .hl { color: var(--text-highlight); }

    /* ── Skills ── */
    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 32px;
    }
    .skill-group-title {
      font-family: 'JetBrains Mono', monospace;
      font-size: 10px;
      color: var(--text-darker);
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 14px;
    }
    .tags { display: flex; flex-wrap: wrap; gap: 7px; }
    .tag {
      font-size: 11px;
      color: var(--text-muted);
      background: var(--bg-card);
      border: 0.5px solid rgba(255,255,255,0.09);
      border-radius: 4px;
      padding: 4px 10px;
    }
    .tag.accent {
      color: var(--accent);
      background: var(--accent-bg);
      border-color: var(--accent-border);
    }

    /* ── Education ── */
    .edu-item { margin-bottom: 24px; }
    .edu-item:last-child { margin-bottom: 0; }
    .edu-degree { font-size: 14px; color: var(--text-highlight); margin-bottom: 4px; }
    .edu-school {
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
      color: var(--text-darker);
    }

    /* ── CTA ── */
    #contact {
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 24px;
      padding-top: 56px;
      padding-bottom: 56px;
    }
    .cta-text { font-size: 22px; font-weight: 500; color: var(--text-primary); line-height: 1.3; }
    .cta-text span { color: var(--accent); }
    .cta-btn {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      background: var(--accent);
      color: #fff;
      border: none;
      border-radius: var(--radius);
      padding: 14px 28px;
      font-size: 13px;
      font-family: 'Inter', sans-serif;
      font-weight: 500;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.2s;
    }
    .cta-btn:hover { background: var(--accent-hover); }
    .cta-btn i { font-size: 16px; }

    /* ── Footer ── */
    footer {
      padding: 24px 48px;
      border-top: 0.5px solid var(--border-soft);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 10px;
    }
    .footer-copy {
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
      color: var(--text-faint);
    }
    .footer-badge {
      font-family: 'JetBrains Mono', monospace;
      font-size: 11px;
      color: var(--text-darker);
      background: rgba(45,125,210,0.06);
      border: 0.5px solid rgba(45,125,210,0.15);
      border-radius: 4px;
      padding: 4px 10px;
    }

    /* ── Responsive ── */
    @media (max-width: 768px) {
      nav, section, footer, #contact { padding-left: 24px; padding-right: 24px; }
      .hero-name { font-size: 36px; }
      #hero { padding-top: 56px; padding-bottom: 56px; }
      .nav-links { gap: 16px; }
    }
    @media (max-width: 480px) {
      .hero-name { font-size: 28px; }
      .metrics-grid { grid-template-columns: repeat(2, 1fr); }
      nav { padding: 16px 20px; }
      section, #contact, footer { padding-left: 20px; padding-right: 20px; }
      .nav-links { display: none; }
    }

    /* ── Focus accessibility ── */
    a:focus-visible, button:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 3px;
      border-radius: 4px;
    }

    @media (prefers-reduced-motion: reduce) {
      * { transition: none !important; scroll-behavior: auto !important; }
    }
  </style>
</head>
<body>

  <nav>
    <a class="nav-logo mono" href="#">roman_pruglo.portfolio</a>
    <div class="nav-links">
      <a href="#experience">Experience</a>
      <a href="#skills">Stack</a>
      <a href="#contact">Contact</a>
    </div>
  </nav>

  <!-- Hero -->
  <section id="hero">
    <div class="hero-eyebrow">Open to relocation · Klaipeda, Lithuania</div>
    <h1 class="hero-name">Roman Pruglo</h1>
    <p class="hero-title">Customer Success Manager · <span>Fintech &amp; Crypto</span> · 7+ years</p>
    <p class="hero-summary">
      Specialising in B2B client retention, churn reduction, and revenue growth. Proven track record managing portfolios of 300–500 clients, running data-driven HADI experiments, and driving upsell outcomes across high-growth environments.
    </p>
    <div class="contacts">
      <a class="chip" href="mailto:kennykennya00@gmail.com"><i class="ti ti-mail"></i>kennykennya00@gmail.com</a>
      <a class="chip" href="tel:+37062605244"><i class="ti ti-phone"></i>+370 626 05244</a>
      <a class="chip" href="https://linkedin.com/in/roman-pruglo" target="_blank" rel="noopener"><i class="ti ti-brand-linkedin"></i>linkedin/roman-pruglo</a>
      <span class="chip"><i class="ti ti-map-pin"></i>Lithuania · EU</span>
    </div>
  </section>

  <!-- Metrics -->
  <section id="metrics">
    <div class="section-label">key impact metrics</div>
    <div class="metrics-grid">
      <div class="metric-card">
        <div class="metric-value mono">10–15%</div>
        <div class="metric-label">Churn reduction per cycle</div>
      </div>
      <div class="metric-card">
        <div class="metric-value mono">300–500</div>
        <div class="metric-label">B2B clients in portfolio</div>
      </div>
      <div class="metric-card">
        <div class="metric-value mono">20+</div>
        <div class="metric-label">Clients onboarded / month</div>
      </div>
      <div class="metric-card">
        <div class="metric-value mono">$5K</div>
        <div class="metric-label">Monthly upsell revenue</div>
      </div>
      <div class="metric-card">
        <div class="metric-value mono">1–2</div>
        <div class="metric-label">HADI experiments / month</div>
      </div>
      <div class="metric-card">
        <div class="metric-value mono">5–7 min</div>
        <div class="metric-label">Avg. ticket response KPI</div>
      </div>
    </div>
  </section>

  <!-- Experience -->
  <section id="experience">
    <div class="section-label">work experience</div>

    <div class="job">
      <div class="job-header">
        <span class="job-title">Customer Success Manager</span>
        <span class="job-period mono">Sep 2025 – Present</span>
      </div>
      <div class="job-company">Tochka Bank</div>
      <ul class="job-bullets">
        <li>Owned portfolio of <span class="hl">300–500 B2B clients</span> with full accountability for retention outcomes</li>
        <li>Reduced churn <span class="hl">10–15% per cycle</span> via data-driven early-intervention playbook — highest risk window: months 2–3 post-onboarding</li>
        <li>Conducted proactive retention calls with at-risk clients — identified root causes of dissatisfaction, addressed objections in real time, and recovered flagged accounts</li>
        <li>Ran <span class="hl">1–2 HADI experiments/month</span>: hypothesis → A/B test → KPI validation → scaling decision</li>
        <li>Used <span class="hl">Tableau</span> daily for pre-test cohort segmentation, live KPI delta tracking, and stakeholder reporting</li>
        <li>Triaged incoming requests, routed to product/tech/finance/compliance, validated solutions before client return</li>
        <li>Escalated recurring issue patterns to product &amp; QMS, converting support tickets into product improvements</li>
      </ul>
    </div>

    <div class="job">
      <div class="job-header">
        <span class="job-title">Foreign Economic Activity Manager</span>
        <span class="job-period mono">Sep 2020 – Aug 2025</span>
      </div>
      <div class="job-company">Tochka Bank</div>
      <ul class="job-bullets">
        <li>Onboarded <span class="hl">20+ B2B clients/month</span> through a structured 6-step process</li>
        <li>Generated <span class="hl">~$5,000/month</span> in upsell revenue; closed 15 upsell deals in a single month</li>
        <li>Negotiated at CEO/CFO level across franchise networks and holding companies</li>
        <li>Segmented client data via <span class="hl">SQL (Oracle/MySQL)</span>; automated reporting with Excel Power Query</li>
        <li>Benchmarked 5+ competitor products per quarter; findings fed directly to product team</li>
      </ul>
    </div>

    <div class="job">
      <div class="job-header">
        <span class="job-title">Client Relationship Manager</span>
        <span class="job-period mono">Apr 2020 – Aug 2020</span>
      </div>
      <div class="job-company">InstaForex · Brokerage</div>
      <ul class="job-bullets">
        <li>Managed 30 active trading clients with a touchpoint every 2 days; drove volume via proactive outreach</li>
        <li>Advised on MT4 setup; conducted risk assessments with 100% compliance</li>
      </ul>
    </div>

    <div class="job">
      <div class="job-header">
        <span class="job-title">Customer Support Specialist</span>
        <span class="job-period mono">Aug 2019 – Feb 2020</span>
      </div>
      <div class="job-company">PrimeXBT · Crypto Derivatives</div>
      <ul class="job-bullets">
        <li>Resolved <span class="hl">30–40 tickets/day</span> (RU/EN) within a 5–7 min response KPI; maintained high CSAT scores</li>
        <li>Explained leverage, margin, liquidation, and on-chain settlements to clients of all experience levels</li>
      </ul>
    </div>
  </section>

  <!-- Skills -->
  <section id="skills">
    <div class="section-label">skills &amp; tools</div>
    <div class="skills-grid">
      <div>
        <div class="skill-group-title">CS &amp; Growth</div>
        <div class="tags">
          <span class="tag accent">Churn Rate</span>
          <span class="tag accent">LTV</span>
          <span class="tag accent">NPS</span>
          <span class="tag accent">NRR</span>
          <span class="tag">Cohort Analysis</span>
          <span class="tag">A/B Testing</span>
          <span class="tag">HADI</span>
          <span class="tag">Upsell / Cross-sell</span>
          <span class="tag">Retention Calls</span>
          <span class="tag">Client Onboarding</span>
          <span class="tag">Portfolio Management</span>
        </div>
      </div>
      <div>
        <div class="skill-group-title">Data &amp; Analytics</div>
        <div class="tags">
          <span class="tag accent">Tableau</span>
          <span class="tag accent">SQL</span>
          <span class="tag">Oracle</span>
          <span class="tag">MySQL</span>
          <span class="tag">Excel Power Query</span>
          <span class="tag">Pivot Tables</span>
        </div>
      </div>
      <div>
        <div class="skill-group-title">CRM &amp; Tools</div>
        <div class="tags">
          <span class="tag">HubSpot</span>
          <span class="tag">MS Dynamics CRM</span>
          <span class="tag">Jira</span>
          <span class="tag">Confluence</span>
          <span class="tag">Miro</span>
          <span class="tag">Notion</span>
          <span class="tag">1C-Bitrix</span>
        </div>
      </div>
      <div>
        <div class="skill-group-title">Languages</div>
        <div class="tags">
          <span class="tag accent">English — C1 Advanced</span>
          <span class="tag">Russian — Native</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Education -->
  <section id="education">
    <div class="section-label">education</div>
    <div class="edu-item">
      <div class="edu-degree">Forensic Economic Expertise / Economic Security</div>
      <div class="edu-school mono">MFUA, Moscow · 2025</div>
    </div>
    <div class="edu-item">
      <div class="edu-degree">Economics · Money, Banks &amp; Financial Markets</div>
      <div class="edu-school mono">RANEPA, Moscow · 2022</div>
    </div>
  </section>

  <!-- CTA -->
  <section id="contact">
    <div class="cta-text">Ready to discuss<br><span>new opportunities</span></div>
    <a class="cta-btn" href="mailto:kennykennya00@gmail.com">
      <i class="ti ti-mail"></i> Get in touch
    </a>
  </section>

  <footer>
    <span class="footer-copy mono">© 2026 Roman Pruglo</span>
    <span class="footer-badge mono">Open to relocation · EU</span>
  </footer>

</body>
</html>
